# Template only -- never commit a real secret here or anywhere in this repo.
# Create the real one out-of-band instead:
#   kubectl create secret generic governed-research-agent-secrets \
#     --from-literal=gemini-api-key=YOUR_GEMINI_KEY \
#     --from-literal=tavily-api-key=YOUR_TAVILY_KEY \
#     --from-literal=api-key=$(openssl rand -hex 32)
apiVersion: v1
kind: Secret
metadata:
  name: governed-research-agent-secrets
type: Opaque
stringData:
  gemini-api-key: "REPLACE_ME"
  tavily-api-key: "REPLACE_ME"
  api-key: "REPLACE_ME"
